# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
# The compiled-in identity facts below must be present in any archive/CI
# checkout; freshness is enforced by the identity --check tooling.
!crates/toktier/data/build/
crates/toktier/data/build/*
!crates/toktier/data/build/source_identity.json
!crates/toktier/data/build/judged_dependencies.lock
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Rust
target/
Cargo.lock.orig

# Native build output
*.so
!src/toktier/_vendor/gigatoken_rs.abi3.so
*.o
*.a

# Editors / OS
.vscode/
.idea/
.DS_Store

# Local scratch
.tmp/
.build/
